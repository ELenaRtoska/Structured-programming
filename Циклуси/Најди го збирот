#include<stdio.h>
int main ()
{
char c;
    int n, suma=0;
   if(scanf("%d", &n))
       suma=n;
    while((c=getchar())&&c!='!')
    {
        if(scanf("%d", &n))
    suma+=n;
    }
    printf("%d", suma);
    return 0;
    
}
